# 의존성 충돌 없는 최소 requirements.txt

# 웹 프레임워크 (유연한 버전)
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart

# PyTorch (기본 버전)
torch
torchvision

# 기본 이미지 처리
opencv-python-headless
Pillow
numpy
requests

# 아직 AI 모델들 제외 (일단 서버 띄우기 우선)